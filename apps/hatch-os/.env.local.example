# Temporary remote (OpenAI-compatible). Host only — never commit the key.
# HATCH_LLM_BASE_URL=https://<host>/v1
# HATCH_LLM_MODEL=qwen3.8
# HATCH_LLM_API_KEY=
# HATCH_LLM_PROVIDER=openai-compatible

# Local fallback (16 GB Mac / Ollama). Used when remote is unset or down.
HATCH_LLM_BASE_URL=http://127.0.0.1:11434
HATCH_LLM_MODEL=qwen3:8b
HATCH_LLM_API_KEY=
HATCH_LLM_PROVIDER=openai-compatible
